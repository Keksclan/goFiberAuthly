# ── Server ────────────────────────────────────────────────────────
# HTTP listen port (default: 8080)
SERVER_PORT=8080

# ── Auth – replace with your auth-server URLs ────────────────────
# Expected JWT issuer (iss claim)
AUTH_ISSUER=https://auth.example.com

# Audience check: "*" accepts any audience, or provide a CSV list
# Examples: "*"  |  "api"  |  "api,admin-portal"
AUTH_AUDIENCE=*

# JWKS endpoint for JWT signature verification
AUTH_JWKS_URL=https://auth.example.com/.well-known/jwks.json

# OAuth 2 token introspection endpoint (optional – leave empty to disable)
AUTH_INTROSPECTION_URL=https://auth.example.com/oauth2/introspect

# Client credentials for introspection Basic Auth (optional)
AUTH_CLIENT_ID=my-service
AUTH_CLIENT_SECRET=supersecret

# Required headers (comma-separated). Requests missing these get 400.
# Example: "x-user-sub" or "x-user-sub,x-tenant-id"
AUTH_REQUIRED_HEADERS=x-user-sub

# ── Config path (optional) ───────────────────────────────────────
# Path to the YAML config file. Default: config.yml in working dir.
# CONFIG_PATH=
